name: Package Smoke

on:
  push:
    branches:
      - main
      - v2-go-with-codex
  pull_request:
  workflow_dispatch:

jobs:
  package-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.29.3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: demo-v2-codex/pnpm-lock.yaml

      - name: Install workspace dependencies
        working-directory: demo-v2-codex
        run: pnpm install --frozen-lockfile

      - name: Build package dist
        working-directory: demo-v2-codex
        run: npm run build:dist

      - name: Pack npm artifact
        working-directory: demo-v2-codex
        run: npm pack --ignore-scripts

      - name: Smoke test fresh app from packed artifact
        run: bash demo-v2-codex/scripts/smoke-create-app.sh --mode local-pack --force --app-dir /tmp/devalbo-create-app-smoke
