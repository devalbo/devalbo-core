# @devalbo/worker-bridge

Worker communication primitives for request/response messaging and shared memory coordination.

## Exports

- protocol types and `createRequestId`
- `WorkerBridge`
- `SharedBufferPool`
- `AtomicsSignal`, `AtomicsLock`
